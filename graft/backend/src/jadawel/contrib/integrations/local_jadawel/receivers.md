# backend/src/jadawel/contrib/integrations/local_jadawel/receivers.py

- invalidate_table_cache · function · L8-L11 — def invalidate_table_cache(sender, table_id, **kwargs): # Invalidate local cache when the table schema is updated
