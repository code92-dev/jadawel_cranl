# web-frontend/modules/core/store/userSource.js

- ADD_ITEM · method · L13-L22 — ADD_ITEM(state, { application, userSource, beforeId = null })
- UPDATE_ITEM · method · L23-L29 — UPDATE_ITEM(state, { application, userSource: userSourceToUpdate, values })
- DELETE_ITEM · method · L30-L37 — DELETE_ITEM(state, { application, userSourceId })
- MOVE_ITEM · method · L38-L44 — MOVE_ITEM(state, { application, index, oldIndex })
- CLEAR_ITEMS · method · L45-L47 — CLEAR_ITEMS(state, { application })
- forceCreate · method · L51-L53 — forceCreate({ commit }, { application, userSource, beforeId = null })
- forceUpdate · method · L54-L56 — forceUpdate({ commit }, { application, userSource, values })
- forceDelete · method · L57-L59 — forceDelete({ commit }, { application, userSourceId })
- forceMove · method · L60-L79 — forceMove( { commit, getters }, { application, userSourceId, beforeUserSourceId } )
- create · method · L80-L95 — async create( { dispatch }, { application, userSourceType, values, beforeId = null } )
- update · method · L96-L135 — async update({ dispatch, getters }, { application, userSourceId, values })
- debouncedUpdate · method · L137-L191 — async debouncedUpdate( { dispatch, getters }, { application, userSourceId, values } )
- fire · function · L161-L175 — fire = async ()
- delete · method · L192-L216 — async delete({ dispatch, getters }, { application, userSourceId })
- fetch · method · L217-L231 — async fetch({ dispatch, commit }, { application })
- move · method · L233-L251 — async move({ dispatch }, { application, userSourceId, beforeUserSourceId })
- duplicate · method · L252-L261 — async duplicate({ getters, dispatch }, { application, userSourceId })
