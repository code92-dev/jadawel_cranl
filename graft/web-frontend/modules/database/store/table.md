# web-frontend/modules/database/store/table.js

- populateTable · function · L9-L17 — function populateTable(table)
- state · function · L19-L24 — state = ()
- ADD_ITEM · method · L27-L29 — ADD_ITEM(state, { database, table })
- UPDATE_ITEM · method · L30-L32 — UPDATE_ITEM(state, { table, values })
- ORDER_TABLES · method · L33-L39 — ORDER_TABLES(state, { database, order, isHashed = false })
- SET_SELECTED · method · L40-L46 — SET_SELECTED(state, { database, table })
- UNSELECT · method · L47-L53 — UNSELECT(state)
- DELETE_ITEM · method · L54-L57 — DELETE_ITEM(state, { database, id })
- SET_LOADING · method · L58-L60 — SET_LOADING(state, value)
- setLoading · method · L64-L66 — setLoading({ commit }, value)
- create · method · L73-L105 — async create( { commit, dispatch }, { database, values, initialData = null, firstRowHeader = true, onUploadProgress = null, } )
- fetch · method · L109-L122 — async fetch({ commit, dispatch }, { database, tableId })
- forceUpsert · method · L127-L135 — forceUpsert({ commit }, { database, data })
- update · method · L139-L149 — async update({ commit, dispatch }, { database, table, values })
- forceUpdate · method · L153-L155 — forceUpdate({ commit }, { database, table, values })
- order · method · L159-L172 — async order( { commit, getters }, { database, order, oldOrder, isHashed = false } )
- delete · method · L176-L188 — async delete({ commit, dispatch }, { database, table })
- forceDelete · method · L193-L209 — forceDelete(context, { database, table })
- select · method · L215-L223 — async select({ commit, dispatch, getters }, { database, table })
- forceSelect · method · L224-L232 — forceSelect({ commit, dispatch }, { database, table })
- selectById · method · L239-L265 — async selectById( { dispatch, getters, rootGetters }, { databaseId, tableId } )
- unselect · method · L269-L276 — unselect({ commit, dispatch, getters })
- getSelectedId · method · L283-L285 — getSelectedId(state)
