# web-frontend/modules/database/services/view/form.js

- prepareRequestConfig · function · L4-L10 — prepareRequestConfig = ({ publicAuthToken = null })
- getMetaInformation · method · L13-L16 — getMetaInformation(slug, publicAuthToken = null)
- submit · method · L17-L20 — submit(slug, values, publicAuthToken = null)
- getEditRow · method · L21-L27 — getEditRow(slug, editToken, publicAuthToken = null)
- submitEditRow · method · L28-L35 — submitEditRow(slug, editToken, values, publicAuthToken = null)
- uploadFile · method · L36-L61 — uploadFile( file, onUploadProgress = function () {}, slug, publicAuthToken = null )
