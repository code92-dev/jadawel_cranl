# web-frontend/test/fixtures/gallery.js

- createGalleryView · function · L1-L35 — function createGalleryView( mock, application, table, { viewType = 'gallery', viewId = 1, filters = [], sortings = [], decorations = [], } )
- createGalleryRows · function · L37-L50 — function createGalleryRows(mock, view, fields, rows = [])
