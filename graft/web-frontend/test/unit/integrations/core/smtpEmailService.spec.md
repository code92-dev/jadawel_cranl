# web-frontend/test/unit/integrations/core/smtpEmailService.spec.js

- mountComponent · function · L110-L143 — async function mountComponent({ service = defaultService, defaultValues = service, integrations = [], application = { id: 1 }, } = {})
